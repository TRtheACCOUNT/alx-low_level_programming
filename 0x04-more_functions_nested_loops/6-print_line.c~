#include "main.h"

/**
 * print_line - print a line .
 * @n: input .
 * Return: 0 .
 */
void print_line(int n);
{
	int i;
	
	for (i = 1; i <= n; i++)
	{
		if (n <= 0)
		{
			_putchar('\n');
		}
		else
		{
			_putchar(95);
		}
	}
	_putchar('\n');
}
