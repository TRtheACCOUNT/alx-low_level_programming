/*
 * File: 7-print_tebahpla.c
 * 
 */

#include <stdio.h>

/**
 * Print the lowercase alphabet in reverse.
 *
 * 
 */
int main(void)
{
	char letter;

	for (letter = 'z'; letter >= 'a'; letteputchar(letter);

	putchar('\n');

	return (0);
}
