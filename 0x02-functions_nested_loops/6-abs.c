#include "main.h"

/**
 * int _abs - Gives the absolute value of a number .
 * @c: input
 * Return: return the absolute value of the input .
 */
int int _abs(int c)
{
	if (c < 0)
		return (-c);
	else
		return (c);
}
