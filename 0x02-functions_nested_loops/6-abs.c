#include "main.h"

/**
 * int_abs - Gives the absolute value of a number .
 * @c: input
 * Return: return the absolute value of the input .
 */
int int_abs(int c)
{
	return (ab * ((ab > 0) - (ab < 0)));
}
