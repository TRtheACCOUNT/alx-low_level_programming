#include "main.h"

/**
 * _isdigit - multiply a by b .
 * @a: input .
 * @b: input .
 * Return: the product of the two numbers .
 */
int mul(int a, int b);
{
	int prod;

	prod = a * b;

	return (prod);
}
