#include "main.h"

/**
 * _isdigit - checks for a number .
 * @c: input .
 * Return: 1 if c is a number, 0 otherwise  .
 */
int _isdigit(int c)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
