#include "main.h"

/**
 * _islower - Check if the letter is lowercase .
 * Return: 1 if c is lowercase, otherwise 0 .
 * @c: letter
 */
int _islower(int c)
{
	char letter;
	int n = 0;

	if (c >= "a" && c <= "z")
		{Return 1}
        else
		{Return 0}
	_putchar('\n');
}
